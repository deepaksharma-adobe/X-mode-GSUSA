import { Consequences } from "../../types/schema";
import { Context, ExecutableRule, ExecutableRuleSetMetadata } from "../../types/engine";
export declare function createTargetRulesExecutor(rules: Array<ExecutableRule>, metadata: ExecutableRuleSetMetadata): {
    provider: string;
    execute: (context: Context) => Array<Consequences>;
};
