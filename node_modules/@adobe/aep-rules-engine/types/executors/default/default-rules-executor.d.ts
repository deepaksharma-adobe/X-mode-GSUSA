import { Consequences, RulesEngineOptions } from "../../types/schema";
import { Context, ExecutableRule } from "../../types/engine";
export declare function createDefaultRulesExecutor(rules: Array<ExecutableRule>, options: RulesEngineOptions): {
    provider: string;
    execute: (context: Context) => Array<Consequences>;
};
