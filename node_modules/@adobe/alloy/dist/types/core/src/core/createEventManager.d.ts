declare function _default({ config, logger, lifecycle, consent, createEvent, createDataCollectionRequestPayload, createDataCollectionRequest, sendEdgeNetworkRequest, applyResponse, }: {
    config: any;
    logger: any;
    lifecycle: any;
    consent: any;
    createEvent: any;
    createDataCollectionRequestPayload: any;
    createDataCollectionRequest: any;
    sendEdgeNetworkRequest: any;
    applyResponse: any;
}): {
    createEvent: any;
    /**
     * Sends an event. This includes running the event and payload through the
     * appropriate lifecycle hooks, sending the request to the server, and
     * handling the response.
     * @param {Object} event This will be JSON stringified and used inside the
     * request payload.
     * @param {Object} [options] Options to pass on to the onBeforeEvent
     * lifecycle method
     * @param {Object} [options.edgeConfigOverrides] Settings that take
     * precedence over the global datastream configuration, including which
     * datastream to use.
     * @returns {*}
     */
    sendEvent(event: Object, options?: {
        edgeConfigOverrides?: Object | undefined;
    }): any;
    applyResponse(event: any, options?: {}): any;
};
export default _default;
