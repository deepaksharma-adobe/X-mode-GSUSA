export default createMediaAnalyticsBridge;
declare function createMediaAnalyticsBridge({ eventManager, sendEdgeNetworkRequest, config, logger, consent, }: {
    eventManager: any;
    sendEdgeNetworkRequest: any;
    config: any;
    logger: any;
    consent: any;
}): {
    lifecycle: {
        onBeforeEvent({ mediaOptions, onResponse }: {
            mediaOptions: any;
            onResponse?: (() => void) | undefined;
        }): void;
    };
    commands: {
        getMediaAnalyticsTracker: {
            run: () => Promise<any>;
        };
    };
};
declare namespace createMediaAnalyticsBridge {
    let namespace: string;
}
