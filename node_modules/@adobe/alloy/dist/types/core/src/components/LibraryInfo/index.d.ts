export default createLibraryInfo;
declare function createLibraryInfo({ config, componentRegistry }: {
    config: any;
    componentRegistry: any;
}): {
    commands: {
        getLibraryInfo: {
            run: () => {
                libraryInfo: {
                    version: string;
                    configs: any;
                    commands: any[];
                    components: any;
                };
            };
        };
    };
};
declare namespace createLibraryInfo {
    let namespace: string;
}
