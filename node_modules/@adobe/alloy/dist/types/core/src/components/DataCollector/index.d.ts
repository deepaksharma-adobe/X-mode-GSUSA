export default createDataCollector;
declare function createDataCollector({ eventManager, logger }: {
    eventManager: any;
    logger: any;
}): {
    commands: {
        sendEvent: {
            documentationUri: string;
            optionsValidator: (options: any) => any;
            run: (options: any) => any;
        };
        applyResponse: {
            documentationUri: string;
            optionsValidator: (options: any) => any;
            run: (options: any) => any;
        };
    };
};
declare namespace createDataCollector {
    let namespace: string;
}
