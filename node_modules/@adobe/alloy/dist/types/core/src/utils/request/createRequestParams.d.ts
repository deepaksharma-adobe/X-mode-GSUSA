declare function _default({ localConfigOverrides, globalConfigOverrides, payload }: {
    localConfigOverrides: Override;
    globalConfigOverrides: Override;
    payload: RequestPayload;
}): {
    payload: RequestPayload;
    datastreamIdOverride: string;
};
export default _default;
export type Override = {
    datastreamId: string;
    [k: string]: Object;
};
export type RequestPayload = {
    mergeConfigOverride: (arg0: Override) => void;
};
