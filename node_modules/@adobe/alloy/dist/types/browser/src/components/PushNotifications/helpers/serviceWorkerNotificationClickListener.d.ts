declare function _default({ makeSendServiceWorkerTrackingData, sw, logger }: {
    makeSendServiceWorkerTrackingData: (options: {
        xdm: Object;
        actionLabel?: string;
        applicationLaunches?: number;
    }) => Promise<boolean>;
    sw: ServiceWorkerGlobalScope;
    logger: ServiceWorkerLogger;
}): ({ event }: {
    event: NotificationEvent;
}) => void;
export default _default;
import type { ServiceWorkerLogger } from '../types.js';
