export default createContext;
declare function createContext({ config, logger }: {
    config: any;
    logger: any;
}): {
    namespace: string;
    lifecycle: {
        onBeforeEvent({ event }: {
            event: any;
        }): Promise<any[]>;
    };
};
declare namespace createContext {
    let namespace: string;
    let configValidators: Function;
}
