export function createMakeSendServiceWorkerTrackingData({ readFromIndexedDb, uuidv4, logger, fetch, }: {
    readFromIndexedDb: (logger: ServiceWorkerLogger) => Promise<Object>;
    uuidv4: () => string;
    logger: ServiceWorkerLogger;
    fetch: (url: string, options: object) => Promise<Response>;
}): (options: {
    xdm: Object;
    actionLabel?: string;
    applicationLaunches?: number;
}) => Promise<boolean>;
import type { ServiceWorkerLogger } from '../types.js';
