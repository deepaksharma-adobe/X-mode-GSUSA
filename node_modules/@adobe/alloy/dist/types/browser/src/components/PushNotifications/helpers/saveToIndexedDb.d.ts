/**
 * @param {Object} data
 * @param {Logger} logger
 *
 * @returns {Promise<void>}
 */
export default function saveToIndexedDB(data: Object, logger: Logger): Promise<void>;
import type { Logger } from '@adobe/alloy-core/core/types.js';
